apiVersion: ops.yiiong.top/v1
kind: IPBlock
metadata:
  labels:
    app.kubernetes.io/name: ipblock-operator
    app.kubernetes.io/managed-by: kustomize
  name: ipblock-sample
spec:
  # TODO(user): Add fields here
