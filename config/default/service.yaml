apiVersion: v1
kind: Service
metadata:
  name: ipblock-trigger
  namespace: default
spec:
  selector:
    app: ipblock-operator
  type: NodePort
  ports:
    - name: grafana
      port: 8090              # Service 对外暴露端口
      targetPort: 8090        # Pod 内 trigger 监听的端口
